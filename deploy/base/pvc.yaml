apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wackorder-bot-data
  labels:
    app.kubernetes.io/name: wackorder-bot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
